<apex:page controller="BuccodecMetricGraphController" showHeader="false" sidebar="false" title="{!chartTitle}">
    <apex:pageBlock title="{!chartTitle}">
    </apex:pageBlock>
    <apex:image url="{!chartData}"></apex:image>
</apex:page>